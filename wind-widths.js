// Пределы ширины страницы. Отдельный файл, а не константа в раскладке: его
// читает и панель кастомизации, и дашборд, а тянуть ради одной таблицы весь
// модуль раскладки с его хуками — лишняя связь между несвязанными вещами.
//
// Ноль означает «без предела». Писать сюда 99999 значило бы придумать предел
// там, где его нет, и однажды в него упереться.
export const WIDTHS = {
  normal: { label: "обычная", px: 1080, hint: "1080 px — как было" },
  wide:   { label: "широкая", px: 1500, hint: "1500 px" },
  full:   { label: "во весь экран", px: 0, hint: "без предела" },
};
